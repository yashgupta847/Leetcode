class Solution {
    public int countCommas(int n) {

        // String s = Integer.toString(n);
        // if (s.length() <= 3)
        //     return 0;
        // // return s.length() / 4;
        return Math.max(0, n - 999);
    }
}